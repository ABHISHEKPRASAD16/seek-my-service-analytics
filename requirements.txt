# Seek My Service - pinned dependencies (Python 3.12, Windows)
# Install:  .venv\Scripts\python.exe -m pip install -r requirements.txt

# --- data generation & analysis ---
numpy==2.1.3
pandas==2.2.3
python-dateutil==2.9.0.post0

# --- machine learning ---
scikit-learn==1.5.2
lightgbm==4.5.0
joblib==1.4.2

# --- model serving ---
# fastapi is pinned at 0.141.1 rather than 0.115.x on purpose. Streamlit
# requires starlette >= 0.46, and fastapi 0.115.x caps starlette below 0.42 -
# the two are mutually exclusive. Installing streamlit alongside the older
# fastapi silently breaks every TestClient in the suite (29 errors, all in
# tests/test_services.py) while pip reports success. Upgrading fastapi is the
# fix; downgrading starlette would break streamlit instead.
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.34.0
pydantic==2.10.4

# --- dashboard ---
streamlit==1.62.0
plotly==6.9.0

# --- testing ---
pytest==8.3.4
httpx==0.28.1
